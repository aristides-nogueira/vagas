export default (theme) => ({
  main: {
    display: 'flex',
    flexDirection: 'column',
  },
  feedbackForm: {
    display: 'flex',
    flexDirection: 'column',
    padding: theme.spacing(2, 0),
  },
  formControl: {
    padding: theme.spacing(2, 0),
  },
  formField: {
    width: '100%',
  },
});
